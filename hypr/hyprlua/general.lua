local mat = require("colors")
hl.config (

{
    general = {
        gaps_out = 30,
        gaps_in = 7,
        border_size = 1,
        col = {
            inactive_border = mat.primary,
            active_border = mat.surface
        }
    }
}

)

